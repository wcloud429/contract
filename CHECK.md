# CHECK — 점검 결과 종합

> 근거 문서: [PLAN.md](PLAN.md) · [DESIGN.md](DESIGN.md) · [CLAUDE.md](CLAUDE.md)
> 작성일: 2026-07-23 · PDCA 단계: Check
> 대상: 국가·지방 계약법령 비교 서비스 (단일 HTML + data.json)

---

## 판정: ✅ 통과

- **성공 기준 5개 전부 통과** (아래 §1). 실데이터 렌더·좌우 고정·배지·PC 남색 화면 정상, 콘솔 에러 0건.
- **설계 ↔ 구현 Gap: 남은 미해결 없음** (아래 §2). 이전 사이클의 Gap은 모두 반영 완료, DESIGN.md도 현재 구현과 일치하도록 갱신됨.
- **보안 점검: 코드 취약점 없음** (아래 §3). XSS·인젝션·개인정보·프롬프트 인젝션 위험 없음. High 위험(키·구조 노출)은 배포 폴더 분리로 구조적 차단 완료.
- **남은 항목은 전부 "코드 밖"** — 호스트 설정(HTTPS·헤더)과 담당자의 조문 매핑 실무 검토뿐. 아래 §4 우선순위 참조.

---

## 1. 성공 기준 판정 (PLAN.md §성공 기준)

| # | 기준 | 판정 | 근거 |
|---|------|------|------|
| 1 | 드롭다운에서 주제를 고르면 3계층 비교가 표시된다 | ✅ 통과 | 5개 주제 로드→선택 시 법률·시행령·시행규칙 블록 렌더 확인 |
| 2 | 항상 좌=국가 / 우=지방 순서 고정 | ✅ 통과 | 각 계층 국가 칼럼 먼저, 지방 뒤 (renderComparison) |
| 3 | 서로 다른 문구를 단어 단위로 판정 | ✅ 통과 | diffWords(LCS)로 "차이 없음" 판정 + 핵심 차이 요약 텍스트 |
| 4 | 대응 조항 없으면 "해당 조항 없음", 완전 동일하면 "차이 없음" 배지 | ✅ 통과 | null→"해당 조항 없음", 동일→배지 표시 |
| 5 | 남색 계열 깔끔한 PC 화면 | ✅ 통과 | 남색 토큰·좌우 2칼럼, 실데이터는 `node serve.js` 필요 |

> 실데이터 확인은 로컬 서버(`node serve.js` → http://127.0.0.1:8000) 필수. file:// 직접 열기 시 안내 표시.

---

## 2. 설계 ↔ 구현 Gap 정리

**현재 미해결 Gap: 없음.** 이전 Check 사이클에서 발견된 Gap은 모두 반영되었고, 그에 맞춰 DESIGN.md도 갱신됨. 주요 반영 이력:

| 항목 | 조치 | 상태 |
|------|------|------|
| 색상 하이라이트 잔재 | 노란색 강조 완전 제거, 분류 대조 표 앰버 행으로 대체 | ✅ 반영 |
| 별표/조문 파싱 정확도 | stripHtml·별표 줄바꿈 복원·별표 제목 매칭 강화 | ✅ 반영 |
| 준용·용어 표기로 인한 허위 차이 | aspectCompare 준용 처리 + 키워드 정규화(증권⊇상장, 공공기관⊇공기업) | ✅ 반영 |
| 계약보증금 지방 시행령 매핑 | 제52조(납부방법)→**제51조**(요율 100분의 10)로 수정 | ✅ 반영 |
| 계약보증금 신설 조항 누락(집행정지 후 추가 납부) | `extraDepositOnStay`로 감지해 핵심 차이에 "집행정지 후 낙찰 시 추가 납부"(국가 신설 100분의 10 vs 지방 미규정) 대조 행 추가 (2026-07-24) | ✅ 반영 |
| 핵심 차이 규모 가시성 | 대조 표 제목에 "차이 N건" 배지 추가(buildBreakdownTable, 5주제 공통) (2026-07-24) | ✅ 반영 |
| 3계층 통합 요약 구조 | 계층별 반복 → mergeSides 통합 요약 1개로 전환, DESIGN §2·§5 갱신 | ✅ 반영 |
| 죽은 코드 | renderWords·escapeHtml 등 미사용 함수 제거 | ✅ 반영 |

> 참고 대기 항목(Gap 아님): **B6 조문 매핑 실무 검토** — DESIGN.md §7 매핑표를 담당자가 확인 후 필요 시 `fetch-laws.js`의 `TOPICS` 수정. (§4 우선순위 참조)

---

## 3. 보안 점검 결과 (인터넷 공개 전)

점검 범위: 키 노출 / 사용자 입력 신뢰(XSS) / 프롬프트 인젝션 / 개인정보 + 정적 호스팅 일반 위험.

### 심각도별

| 심각도 | 항목 | 상태 |
|--------|------|------|
| 🔴 Critical | 없음 | — |
| 🟠 High | **H-1** `.env` 키가 폴더 통째 업로드 시 유출 | ✅ 조치 — `dist/`에 index.html+data.json만 분리 |
| 🟠 High | **H-2** 빌드·개발 파일 혼입 시 내부 구조 노출 | ✅ 조치 — `dist/` 화이트리스트 배포 + CLAUDE.md 규칙화 |
| 🟡 Medium | **M-1** CSP·보안 헤더 부재 | ✅ 조치(코드분) — index.html에 CSP `<meta>` 추가. 호스트 헤더는 §4 잔여 |
| 🟢 Low | **L-1** serve.js 경로 이탈 접두어 우회(로컬 전용) | ✅ 조치 — `startsWith(ROOT + path.sep)` |
| 🟢 Low | **L-2** HTTPS 강제 | ⏳ 잔여 — 호스트 설정 (§4) |

### 안전 확인 (위험 없음)

- **① 비밀 키**: data.json에 키 0건, 화면/브라우저 노출 경로 없음. `.gitignore`의 `.env` 보호 정상, 현재 커밋 0건.
- **② 사용자 입력(XSS)**: 외부 데이터(조문·별표·주제 title)는 전부 `textContent` 삽입. `innerHTML`은 개발자 하드코딩 상수뿐 → 인젝션 경로 없음.
- **③ 프롬프트 인젝션**: **해당 없음** — LLM/챗봇 없음. 조문을 외부/LLM으로 전송 안 함. `fetch`는 로컬 `./data.json` 하나뿐.
- **④ 개인정보**: 입력은 주제 선택 `<select>`뿐. localStorage/쿠키/외부 전송/애널리틱스 전무. 공개 법령만 취급.
- **외부 리소스**: CDN·폰트·이미지 없이 완전 자기완결.

---

## 4. 고쳐야 할 항목 (우선순위 순)

이미 반영된 것은 위 표에 ✅로 표기했고, **아직 남은 조치만** 순서대로 정리한다.

| 순위 | 항목 | 심각도 | 무엇을 / 왜 | 담당 |
|------|------|--------|-------------|------|
| 1 | **배포는 `dist/`만 올리기** | High(H-1/H-2) | ✅ `node build-dist.js`로 자동화 — 허용 목록(index.html+data.json)만 결정적으로 복사, 비밀 파일 혼입 불가. 배포 땐 이 dist/만 업로드 | 배포 시점(사용자) |
| 2 | **호스트 HTTPS 강제 + 보안 헤더** | Medium/Low(M-1/L-2) | 호스트에서 "Enforce HTTPS", `X-Content-Type-Options: nosniff`, HSTS 켜기. 코드가 아니라 호스트 설정이라 앱에서 못 함 | 배포 시점(사용자) |
| 3 | **B6 조문 매핑 실무 검토** | (정확성) | DESIGN.md §7 매핑표(5주제×3계층)를 담당자가 검토 후 필요 시 `TOPICS` 수정 | 계약제도 담당자 |

> 1·2번은 "인터넷 공개" 시점에만 필요한 작업이며, 앱 코드 자체는 공개 준비 완료 상태.

---

## 결론

- **코드 품질·설계 일치·보안: 통과.** 앱 자체는 인터넷 공개 준비가 끝났다.
- **공개 시 반드시 지킬 것**: `dist/` 2개 파일만 배포 + 호스트 HTTPS/헤더 설정.
- **정확성 관련 잔여**: 조문 매핑은 법령 실무자의 최종 확인을 권장(비교 도구이며 유권해석은 하지 않음).
