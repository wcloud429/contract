# CLAUDE.md

이 파일은 이 저장소에서 코드 작업을 할 때 Claude Code(claude.ai/code)를 위한 지침을 제공합니다.

## 협업 규칙 (작업 시 항상 지킬 것)

- **언어**: 모든 설명과 코드 주석은 **한국어**로 작성한다.
- **파일 위치**: 새 파일은 반드시 `my-app` 폴더 안에만 만든다. 폴더 밖에 만들지 않는다.
- **변경 보고**: 코드를 바꾸면 반드시 **무엇을 왜 바꿨는지 한 줄로** 알려준다.
- **비밀 정보**: `.env` 등 비밀 정보 파일은 `.gitignore`에 등록해 두고, **절대 커밋하지 않는다.**
- **삭제 금지 (휴지통 방식)**: 파일을 지워야 할 때는 바로 삭제하지 않는다. `trash-can` 폴더를 만들어 그 안으로 **옮겨만 둔다.** 최종 삭제는 사용자가 직접 확인하고 진행한다.
- **서브에이전트 활용**: 이미 설치된 서브에이전트(bkit의 `gap-detector` 등)를 필요할 때마다 적극 활용한다.

## 작업 절차 (검증 루프)

코드 작업은 아래 루프를 **매번 반복**한다. 통과할 때까지 돌린다.

1. **변경한다** — 코드를 수정한다.
2. **결과를 직접 확인한다** — HTML 파일을 브라우저로 열어(또는 실행해) 실제 동작을 눈으로 확인한다.
3. **스스로 코드 리뷰한다** — 도메인 규칙·협업 규칙 준수 여부와 버그를 스스로 점검한다.
4. **문제가 있으면 고치고 다시 1)로 돌아간다.**
5. **통과하면** — 무엇을 왜 바꿨는지 **한 줄로 요약**한다.

## 프로젝트 개요

**국가·지방 계약법령 비교 서비스** — 국가계약법령과 지방계약법령의 같은 주제 조항을 법률·시행령·시행규칙 3계층으로 좌우 비교하고, 두 조항의 차이를 '핵심 차이 요약'(사실 기반)으로 한눈에 확인하게 하는 웹앱.

- **사용자**: 재정경제부·행정안전부의 계약제도 담당자
- **해결하는 문제**: 두 법령을 관장하는 부처가 달라 조항을 일일이 수작업으로 대조해야 하는 불편
- **범위**: '비교'만 한다. 법률 해석·자문은 하지 않는다(해석은 사람의 몫).
- 상세 기획은 [PRD.md](PRD.md), 요약본은 [prd_lite.md](prd_lite.md) 참고.

## 기술 스택 & 아키텍처

- **순수 프론트엔드 + 빌드타임 수집**: 화면은 `index.html`(HTML+CSS+순수 JS). 프레임워크·빌드·패키지 매니저 없음.
- **의존성 없음**: 외부 라이브러리·CDN·npm 패키지 없음. diff(LCS) 로직도 직접 구현.
- **구성 파일**:
  - `index.html` — 비교 화면. `data.json`을 `fetch`해 렌더. **주제 선택 → 법률·시행령·시행규칙 3계층**을 한 화면에, 계층마다 좌=국가/우=지방 비교.
  - `data.json` — 법령 조문 데이터(계층 중첩 구조). 공개 조문만 담김(키 없음).
  - `fetch-laws.js` — 법제처 API 수집 스크립트(Node). 6개 법령 캐시 후 `TOPICS` 조문번호로 추출 → `data.json` 생성. 실행: `node fetch-laws.js`.
  - `serve.js` — 로컬 확인용 정적 서버(포트 8000, `.env` 서빙 차단).
- **데이터 소스**: **법제처(국가법령정보 공동활용) OpenAPI** — **A안(빌드타임 수집)** 채택·구현 완료. 키 노출·CORS 때문에 브라우저 직접 호출(C)·프록시(B)는 미채택. 상세는 [DESIGN.md](DESIGN.md) §7.

## 실행 방법

- **로컬 서버로 연다**: `node serve.js` → 브라우저에서 `http://127.0.0.1:8000/`.
- `index.html`이 `data.json`을 `fetch`하므로 **file:// 로 더블클릭하면 실데이터가 안 뜨고 안내가 표시**된다(브라우저 보안). 실데이터 확인은 서버 필수.
- 법령 데이터 갱신: `node fetch-laws.js` 재실행 → `data.json` 갱신 → 새로고침.
- 이 환경 참고: 한글 OneDrive 경로 때문에 일부 프리뷰 도구의 file:// 로딩이 막힐 수 있어, 확인은 Node 정적 서버(`serve.js`)를 쓴다.

## 도메인 규칙 (반드시 지킬 것)

이 앱의 핵심 가치는 아래 규칙의 정확한 준수에 있다.

### 좌우 비교 뷰
- **좌측 = 국가계약법령, 우측 = 지방계약법령** 순서를 항상 고정한다.
- 한 화면에는 **한 개의 비교 주제**를 표시하되, 그 주제의 **법률·시행령·시행규칙 3계층**을 세로로 쌓아 계층마다 국가/지방을 좌우 비교한다.
- 조항은 조문 번호가 아니라 **'비교 주제'를 기준으로** 짝짓는다.
- 긴 조문도 별도 내부 스크롤 없이 세로로 전체를 펼쳐 표시한다.
- 표·별표 조항도 텍스트로 풀어 비교 대상에 포함한다.
- 대응 조항이 없으면 빈 칸이 아니라 **"해당 조항 없음"** 을 명시한다.

### 차이 표시 (핵심 차이 요약)
- 차이는 **단어 단위 LCS**로 판정한다. (초기의 색상 하이라이트는 **제거**되었고 텍스트 요약으로 대체됨)
- 각 계층 위에 **핵심 차이 요약** 박스를 둔다: 서로 다른 구간을 성격별(**기간·기한/비율·금액/사유·대상/부과·납부/문구·용어**)로 분류해 나열한다. 분류는 키워드 기준 참고용.
- 개정·신설·삭제 등 `<...>` 표기는 요약 대조에서 **제외**한다.
- **의미·유불리는 판단하지 않는다**(유권해석 금지). "※ 문구 대조 결과이며 법적 해석이 아닙니다." 를 명시한다.
- 조문이 참조하는 **별표**는 접이식으로 함께 표시한다.
- 두 조문이 완전히 같으면 **"차이 없음" 배지**, 한쪽만 있으면 "해당 조항 없음"으로 표시한다.

### 주제 선택
- 주제를 고르는 **드롭다운(선택 UI)** 을 반드시 제공하고, 선택한 주제의 3계층 비교를 표시한다.
- 초기 비교 주제는 제재 관련 조항: **입찰참가자격 제한, 과징금, 지체상금(지연배상금)** 등.

## 디자인 가이드

- **분위기**: 공공기관 실무용답게 깔끔하고 신뢰감 있는 정돈된 문서 느낌. 화려함보다 **가독성 우선**.
- **메인 색상**: 차분한 **남색(네이비/딥블루) 계열**. 단, 차이 하이라이트 색과는 명확히 구분되게 한다.
- **화면**: 좌우 나란히 보는 구조상 **PC(데스크톱) 화면 기준으로 최적화**. 모바일 대응은 이번 범위 아님.

## 비범위 (만들지 말 것)

- 국가·지방 계약법령 외 다른 법령(세법·하도급법 등) 비교
- 조항 자동 개정·업데이트 크롤링
- AI 자동 유권해석·법률 자문
- 회원가입·로그인·권한관리 등 계정 시스템

## 보안·데이터

- 다루는 법령 조문은 법제처의 **공개 정보**이며, **개인정보를 수집·저장·처리하지 않는다.**
- **법제처 API 키(OC 등) 관리 규칙**:
  - 키는 **절대 소스 코드·HTML·커밋에 넣지 않는다.** 오직 `.env` 파일에만 둔다(`.gitignore`로 보호, 템플릿은 [.env.example](.env.example)).
  - 순수 클라이언트 HTML에는 키를 두지 않는다(브라우저에 노출됨). 키는 로컬 스크립트/서버 등 **키가 노출되지 않는 경계**에서만 사용한다.
  - 키가 실수로 커밋·업로드되면 즉시 폐기(재발급)하도록 사용자에게 알린다.
- **배포 규칙 (인터넷 공개 시)**: 폴더를 통째로 올리지 않는다. 반드시 **`dist/` 폴더의 `index.html` + `data.json` 2개만** 호스트에 올린다. `.env`·`fetch-laws.js`·`serve.js`·`.git`·`*.md`·`trash-can` 등은 절대 배포에 포함하지 않는다(키·내부 구조 노출 방지). 데이터 갱신 시 `node fetch-laws.js` → 루트 `data.json` 갱신 → `dist/`에 다시 복사.
- **dist 최신화 (반복 실수 방지)**: `index.html` 또는 `data.json`을 고치면 **루트 파일이 최신이고 `dist/`는 낡은 상태**가 된다. 배포 직전에는 반드시 **`node build-dist.js`를 다시 실행**해 `dist/`를 재생성한 뒤 올린다. (루트에서 확인·검증하고, 배포용으로만 dist를 다시 빌드한다.)
