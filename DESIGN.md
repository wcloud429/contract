# DESIGN — 국가·지방 계약법령 비교 서비스

> 근거 문서: [PRD.md](PRD.md) · [PLAN.md](PLAN.md)
> 작성일: 2026-07-23 · PDCA 단계: Design

이 문서는 [PLAN.md](PLAN.md)의 9개 작업을 **어떤 구조와 방식으로 구현할지**를 정한다.
단일 HTML 파일·순수 JS 구성이므로 아키텍처는 단일 파일로 고정하고, 그 내부 구조를 구체화한다.

---

## 1. 전체 구조

- **파일**: `index.html` **1개**. 외부 라이브러리·빌드·서버 없음.
- **파일 내부 3영역**:
  1. `<style>` — 남색 계열 디자인 토큰과 레이아웃
  2. `<body>` 마크업 — 헤더 · 주제선택 바 · 좌우 비교 영역
  3. `<script>` — `data.json` 로드 → `LAW_DATA` + 드롭다운/계층 렌더/diff 로직

## 2. 마크업 골격 (변경됨 · 3계층 구조)

```
header            제목 + 설명 + 데이터 출처 안내(#data-notice)
main
 ├ .topic-bar      "비교 주제" 라벨 + <select id="topic-select">
 └ #compare-root
      ├ .tier-summary  ← 핵심 차이 '통합' 표 1개(최상단). 3계층을 mergeSides로 합쳐 산출
      ├ .trend-panel   ← '동향 살펴보기' 접이식(details, 기본 접힘). 관련 조문 안내 + 공식 출처 링크
      └ .tier (×3: 법률/시행령/시행규칙)   ← 조문 좌우 비교만
           ├ .tier-head   계층명 + "차이 없음" 배지
           └ .compare     display:grid, 2칼럼
                ├ .column  국가 법령 + 조문번호 + 조문 (+ 참조 별표 details)  ← 항상 좌측
                └ .column  지방 법령 + 조문번호 + 조문 (+ 참조 별표 details)  ← 항상 우측
```
(변경됨: 초기엔 계층별 `.tier-summary`였으나, 현재는 3계층 통합 요약 1개를 `#compare-root` 최상단에 두고 아래에 계층별 조문만 나열)

- **좌우 순서 고정**: 각 계층 블록에서 국가 칼럼을 항상 먼저, 지방 칼럼을 뒤에 둔다.
- **주제 선택 → 3계층 한 화면**: 주제 하나를 고르면 법률·시행령·시행규칙 3개 계층 블록이 세로로 쌓여 각각 국가/지방을 좌우 비교한다.
- **선택 전 상태**: `#compare-root`에 "위에서 비교할 주제를 선택하세요." 안내를 둔다.

## 3. 디자인 토큰 (CSS 변수)

| 변수 | 값 | 용도 |
|------|-----|------|
| `--navy` | `#1a3a6b` | 메인 남색(제목·칼럼 상단선·포커스) |
| `--navy-deep` | `#12294c` | 헤더 배경 |
| `--bg` | `#f4f6fa` | 페이지 배경 |
| `--card` | `#ffffff` | 칼럼/바 배경 |
| `--border` | `#d7deea` | 옅은 테두리 |
| `--text` / `--muted` | `#1f2733` / `#6b7684` | 본문 / 보조 텍스트 |
| (요약 박스) | `#eef3fb` 배경 / `--navy` 태그 | 핵심 차이 요약 박스·카테고리 태그 |
| (배지 색) | `#e6f4ea`/`#1e7d43` (초록) | "차이 없음" 배지 |
| (분류 대조 표) | `#dbe6f5`(헤더) / `#fff7e6`(diff-row 앰버) | `.act-table` 헤더·국가≠지방 행 강조 |

> 참고: 초기의 노란색 조문 하이라이트(`#ffe066`, `mark.diff`)는 **완전히 제거**됨(코드에 없음). 차이 강조는 분류 대조 표(`.act-table`)의 국가≠지방 행 앰버(`#fff7e6`)로만 사용.

- **화면 기준**: PC(데스크톱). `main`은 `max-width` 컨테이너로 가운데 정렬.

## 4. 데이터 구조 (변경됨 · 3계층 중첩)

`data.json`(법제처 API 수집)을 로드해 `LAW_DATA`에 담는다. 주제마다 3계층(tiers)을 가진다.

```js
// data.json
{
  source: "law-api",           // "example" 이면 예시 데이터 안내 표시
  generatedAt: "YYYY-MM-DD",
  topics: [
    {
      id, title,
      tiers: [                  // 법률 / 시행령 / 시행규칙 순서
        {
          key: "law"|"decree"|"rule",
          label: "법률"|"시행령"|"시행규칙",
          articleNo: { national, local },  // 조문번호 표기용 (예: "27의2")
          national: {                      // 국가
            text: string|null,             //   조문 텍스트, 없으면 null → "해당 조항 없음"
            tables: [ { title, content } ] //   조문이 참조하는 별표(고정폭 텍스트), 없으면 []
          },
          local: { text, tables }          // 지방 (구조 동일)
        }
      ]
    }
  ]
}
```
- **소스**: 법제처(국가법령정보 공동활용) API로 수집(§7). `source: "example"`이면 예시 데이터로 간주하고 화면에 안내를 띄운다.

## 5. 구현 방식 (요약)

| 기능 | 구현 방식 |
|------|-----------|
| 데이터 로드 | `fetch("./data.json")` → `LAW_DATA`. 실패 시 안내(로컬 서버로 열기) |
| 드롭다운 | `LAW_DATA`로 `<option>` 채움 (value=id) |
| 계층 렌더 | 주제 선택 → `tiers` 순회하며 계층 블록(.tier) 동적 생성(조문 좌우 비교) |
| 좌우 표시 | 각 계층에서 국가/지방 조문 주입, 없으면 "해당 조항 없음" |
| 별표 표시 | 조문이 참조하는 별표를 `<details>` 접이식(기본 접힘, 펼치면 max-height 스크롤)으로 고정폭 표시 |
| **핵심 차이 '통합' 요약** | (변경) `renderComparison`이 `mergeSides`로 3계층(법률·시행령·시행규칙)을 한 쪽으로 합친 뒤, 통합 요약 표 1개를 `#compare-root` 최상단에 배치. 계층별 요약은 없음 |
| **제재 주제 요약** | 제재 3주제(입찰·과징금·지체)는 `buildTierSummary`(sanctionMap 정량 차원 + `getBreakdown` 분류 대조 표). 입찰=행위별 제재기간, 과징금=사유별 부과율, 지체=계약종류별 요율. `parseCategoryValues`(헤더-리셋 하이브리드)·`contractBreakdown`(항목 분할). **유권해석 금지** 명시 |
| **보증금 주제 요약** | 보증금 2주제는 `buildGuaranteeSummary` — **요율·납부수단·면제/대상** 3측면 키워드 대조(`GUARANTEE_PAY/TGT`, `pickKw`). 표기 정규화(증권⊇상장증권, 공공기관⊇공기업)와 `aspectCompare`의 **준용 처리**(한쪽이 다른 조문 준용 시 허위 차이 방지) |
| 대조 표 | `buildBreakdownTable` — 3열(`act-table`), 국가≠지방 행 앰버 강조(`diff-row`). 행에 `same` 플래그 있으면 강조 제어. **제목 옆 "차이 N건" 배지**(차이 없으면 초록 "차이 없음")로 차이 규모를 한눈에 |
| **동향 살펴보기** | `buildTrendPanel(item)` — 핵심 차이 아래 접이식 패널(`.trend-panel`). 주제별 **관련 조문**(tiers 매핑 + formatArtNo) 안내 + **공식 출처 링크**(`TREND_LINKS`: 법제처 국가법령정보센터·국회 의안정보시스템·국민참여입법센터)를 새 탭(`target=_blank`, `rel=noopener`)으로 연다. **실시간 수집 아님**(CSP `connect-src 'self'`·비범위 '자동 크롤링' 준수) — 담당자가 직접 확인하는 링크 모음. href는 고정 상수 `encodeURI`, 라벨은 textContent(XSS 안전) |
| 배지 | 계층별 조문 텍스트가 완전히 같으면(diffWords) head에 "차이 없음", 한쪽 없으면 표시 안 함 |
| 조문번호 표기 | `"27의2"` → `"제27조의2"` (formatArtNo) |
| 표기 정규화 | 국가 '1천분의' ↔ 지방 '1000분의', '음ㆍ식료품' 가운뎃점 등 정규화(`normRate`, 키워드) |
| (제거됨) 색상 하이라이트 | 초기 노란색 조문 강조는 제거. diff 강조는 분류 대조 표의 행 단위(앰버)로만 사용 |
| 마무리 | 남색 토큰·여백·행간, PC 최적화 |

> **주의**: 초기 설계의 'LCS 변경구간 → 성격별(기간·비율·사유·부과·문구) 분류' 요약은 **폐기**되고, 위 '제재수준 추출 + 주제별 분류 대조 표'로 전면 교체됨. `diffWords`(LCS)는 이제 '차이 없음' 배지 판정에만 쓰인다.

## 6. 이번 작업(1) 완료 기준

- `index.html`을 브라우저로 열면 **제목 + 주제선택 바(빈 상태) + 좌=국가/우=지방 2칼럼**이 남색 스타일로 보인다.
- 아직 데이터·선택·비교 동작은 없다(자리표시만).
- 콘솔 에러 없음.

---

## 7. 법제처 API 연동 아키텍처 (✅ A안 확정·구현 완료)

법령 데이터는 **법제처(국가법령정보 공동활용) OpenAPI**로 수집한다.
순수 클라이언트 HTML은 키 노출·CORS로 직접 호출이 부적합하여, **A안(빌드타임 수집)** 을 채택했다.
(B 프록시 서버 / C 브라우저 직접 호출은 미채택 — 각각 서버 운영 부담 / 키 노출·CORS 문제.)

### 구현 (A안)

- **수집 스크립트** `fetch-laws.js` (Node, 의존성 없음):
  - `.env`에서 `LAW_API_OC`를 읽는다(키는 여기서만 사용, `data.json`에는 미포함).
  - **6개 법령**(3계층 × 국가/지방)을 각각 1회 호출해 캐시하고, `TOPICS`에 정의된 조문번호(가지번호 포함)로 조문을 추출한다.
  - 조문 본문은 `조문내용`(제목 줄) + `항/호/목` 내용을 모아 구성한다.
  - **별표 수집**: 조문이 "별표 N과 같다"처럼 참조하면, 같은 법령 `별표.별표단위`에서 해당 별표(별지 서식 제외)의 `별표내용`을 함께 수집한다. 별표내용은 원본 줄바꿈이 콤마로 치환돼 오므로 줄바꿈으로 복원해 저장(고정폭 표로 표시). 예: 시행규칙 제76조 → 별표 2(제한기준), 제77조의2 → 별표 3·4(과징금 부과기준).
  - 결과를 §4 구조의 `data.json`으로 저장(`source: "law-api"`).
- **런타임** `index.html`: `data.json`을 `fetch`로 읽어 렌더링. file:// 직접 열기는 fetch가 막히므로 로컬 서버(`serve.js`)로 연다.
- **갱신**: `node fetch-laws.js` 재실행 → `data.json` 갱신 → 새로고침.

### 조문 매핑 (담당자 검토 권장)

| 주제 | 법률(국가/지방) | 시행령(국가/지방) | 시행규칙(국가/지방) |
|------|------|------|------|
| 입찰참가자격 제한 | 제27조 / 제31조 | 제76조 / 제92조 | 제76조 / 제76조 |
| 과징금 | 제27조의2 / 제31조의2 | 제76조의2 / 제92조의2 | 제77조의2 / 제77조의2 |
| 지체상금(지연배상금) | 제26조 / 제30조 | 제74조 / 제90조 | 제75조 / 제75조 |
| 입찰보증금 | 제9조 / 제12조 | 제37조 / 제37조 | 제43조 / 제41조 |
| 계약보증금 | 제12조 / 제15조 | 제50조 / **제51조**(계약의 이행보증) | 제51조 / 제49조 |

> 계약보증금 지방 시행령은 제52조(납부방법)가 아니라 **제51조**에 요율(100분의 10)이 규정됨. (공동계약 주제는 추가했다가 삭제됨)
> 주제·조문 추가/수정은 `fetch-laws.js`의 `TIERS`(법령명)와 `TOPICS`(조문번호)만 고치면 된다.

### 관련 파일

- `serve.js` — 로컬 확인용 정적 서버(포트 8000, `.env` 서빙 차단).
- `.env` / `.env.example` — OC 키(gitignore 보호) / 값 없는 템플릿.
