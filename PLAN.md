# PLAN — 국가·지방 계약법령 비교 서비스

> 근거 문서: [PRD.md](PRD.md) (특히 §3 성공 기준, §5 주요 기능, §9 개발 단위)
> 작성일: 2026-07-23 · PDCA 단계: Plan

---

## 이번 사이클 목표

PRD에 정의된 **Must 기능 3가지**(① 조항 좌우 비교, ② 차이점 하이라이트, ③ 비교 주제 선택)를
**단일 HTML 파일** 하나로 완성해, 담당자가 국가계약법령과 지방계약법령의 차이를 한눈에 확인할 수 있게 한다.

## 성공 기준 (달성 현황)

- [x] 드롭다운에서 주제(입찰참가자격 제한·과징금·지체상금 등)를 고르면 비교가 표시된다. *(확장: 이제 법률·시행령·시행규칙 3계층을 한 화면에)*
- [x] 항상 **좌=국가계약법령 / 우=지방계약법령** 순서로 고정되어 나란히 보인다.
- [x] 서로 다른 문구가 **단어 단위**로 판정된다. *(초기: 1색 하이라이트 → 현재: '핵심 차이 요약' 텍스트로 대체, 사이클 3 참조)*
- [x] 대응 조항이 없으면 **"해당 조항 없음"**, 차이가 전혀 없으면 **"차이 없음"** 배지가 표시된다.
- [x] 남색 계열의 깔끔한 PC 화면. *(단, 실데이터는 `data.json` fetch가 필요해 로컬 서버 `serve.js`로 연다 — file:// 직접 열기 시엔 "로컬 서버로 여세요" 안내 표시)*

---

## 작업 순서 (먼저 되면 나머지가 쉬워지는 순)

1. HTML 기본 골격과 페이지 틀(제목·비교 영역 자리)을 만든다 — 뼈대가 있어야 나머지를 얹는다.
2. 법령 데이터를 주제별(국가/지방 1쌍씩) JS 객체로 파일 안에 넣는다 — 데이터가 있어야 표시·비교가 가능하다.
3. 비교 주제를 고르는 드롭다운 UI를 만들고 데이터의 주제 목록으로 채운다.
4. 좌=국가·우=지방 2칼럼 좌우 레이아웃을 만든다.
5. 선택한 주제의 조문을 좌우에 표시하고, 없으면 "해당 조항 없음"을 표시한다.
6. 좌우 두 조문의 텍스트 차이를 찾는 단어 단위 diff 로직을 만든다.
7. 서로 다른 문구만 1가지 색으로 하이라이트한다(같은 문구는 강조하지 않음).
8. 차이가 전혀 없을 때 "차이 없음" 배지를 표시한다.
9. 남색 계열 스타일과 가독성을 다듬어 PC 화면에 맞춰 정리한다.

---

## 추가 사이클 1 — 법제처 API 연동 (✅ 완료 · A안 빌드타임 수집)

- [x] **A0. 아키텍처 방안 확정** — A(빌드타임 수집) 채택.
- [x] **A1. `.env` 세팅** — `LAW_API_OC` 입력 (gitignore 보호).
- [x] **A2. 수집 스크립트** — `fetch-laws.js`가 법제처 API 호출 → `data.json` 저장.
- [x] **A3. 데이터 로딩 교체** — `index.html`이 `data.json`을 `fetch`해서 사용 (실패 시 안내).
- [x] **A4. 출처 표기** — `source`에 따라 "예시 데이터" 안내 / "법제처 API 출처" 자동 전환.
- [x] **A5. 검증** — 실제 조문으로 드롭다운·좌우표시·하이라이트·배지 정상 동작 확인.

## 추가 사이클 2 — 3계층 비교 확장 (✅ 완료)

> 비교 단위를 단일 조문쌍 → **주제별 법률·시행령·시행규칙 3계층 동시 비교**로 확장. 상세는 DESIGN.md §7.

- [x] **B1. 6개 법령 조문 탐색** — 3계층 × 국가/지방에서 주제 조문 위치 확인(추측 아님).
- [x] **B2. `fetch-laws.js` 개편** — `TIERS`(6법령 캐시) + `TOPICS`(계층별 조문번호) → 중첩 `data.json`.
- [x] **B3. `index.html` 개편** — 주제 선택 시 `#compare-root`에 계층 블록 3개 동적 렌더.
- [x] **B4. 조문번호 표기** — `제27조의2` 형식(formatArtNo).
- [x] **B5. 검증** — 3주제×3계층 렌더·조문번호·하이라이트·배지 확인.
- [ ] **B6. (대기) 조문 매핑 실무 검토** — DESIGN.md §7 매핑표를 담당자가 확인 후 필요 시 `TOPICS` 수정.

## 추가 사이클 3 — 별표·핵심 차이 요약 (✅ 완료)

- [x] **C1. 별표 수집·표시** — 조문이 참조하는 별표(제한기준표·과징금 부과기준표)를 API에서 함께 수집(줄바꿈 복원), 접이식 고정폭 블록으로 표시.
- [x] **C2. 색상 하이라이트 제거** — 노란색 강조 제거, 원문 표시로 전환.
- [x] **C3. 핵심 차이 요약** — LCS 정렬로 변경 구간 추출 → 계층별 사실 요약(유권해석 금지 명시).
- [x] **C4. 요약 고도화** — 개정일자(`<...>`) 제외 + 성격별 분류(기간·비율·사유·부과·문구).

## 추가 사이클 4 — 제재수준 요약 개편 + 주제 확장 + 통합 요약 (✅ 완료)

> 자세한 구현은 DESIGN.md §5·§7 참조.

- [x] **D1. 핵심 차이 = 제재수준** — 성격별 분류 → 정량 제재수준(제재기간·과징금율·지체상금률·한도·벌점) + 주제별 분류 대조 표.
- [x] **D2. 주제 확장(3→5)** — 입찰보증금·계약보증금 추가(공동계약은 추가 후 삭제). 계약보증금 지방 시행령 제51조 매핑.
- [x] **D3. 보증금 3측면** — 보증금은 제재가 아니라 요율·납부수단·면제/대상 키워드 대조(표기 정규화 + 준용 처리).
- [x] **D4. 3계층 통합 요약** — 계층별 반복 대신 `mergeSides`로 합쳐 통합 핵심 차이 표 1개를 최상단에.
- [x] **D5. 조문 정제** — 수식 `<img>`·HTML 태그 제거(`stripHtml`).

## 비범위 (이번에 만들지 않음)

- 국가·지방 계약법령 외 다른 법령 비교 / **실시간 상시 자동 갱신·모니터링**(빌드타임·수동 수집까지만) / AI 유권해석 / 회원가입·로그인
