# 국가·지방 계약법령 비교 서비스 기획서 (PRD)

- 작성자: 이주현
- 작성일: 2026-07-23

---

## 0. 구현 현황 (문서 통일 기준)

이 기획서는 초기 계획이며, 구현하며 확장·확정된 사항이 있습니다. **현재 상태 기준 요약:**

- **라이브 데모**: https://my-app-silk-chi-22.vercel.app (Vercel 정적 배포)
- **비교 주제 5개**: 입찰참가자격 제한 · 과징금 · 지체상금 · 입찰보증금 · 계약보증금 (초기 제재 3개 → 보증금 2개 확장)
- **데이터 소스**: 법제처 OpenAPI를 **A안(빌드타임 수집)** 으로 확정·구현 (`fetch-laws.js` → `data.json`)
- **핵심 차이 요약**: 초기 'LCS 성격별 분류'에서 **정량 제재수준 대조**(제재 주제)와 **요율·납부수단·면제/대상 대조**(보증금 주제)로 발전. "차이 N건" 배지 제공.
- **추가 구현**: 참조 별표 접이식 표시, 주제별 **'동향 살펴보기'**(법제처·국회 의안정보시스템 등 공식 출처 링크).
- **보안**: 키는 `.env`에만(커밋 금지), CSP·`textContent` 렌더링, 개인정보 미수집, 배포는 `dist/`(2개 파일)만 + 보안 헤더.

> 상세는 [README.md](README.md)·[DESIGN.md](DESIGN.md), 점검 결과는 [CHECK.md](CHECK.md) 참고. 아래 원문(§1~§9)은 초기 기획 기록으로 두되, 확장·변경된 부분은 '(변경됨)'으로 표기합니다.

---

## 1. 추진 배경 (문제 정의)
*누가, 어떤 업무에서, 어떤 불편을 겪는지 적는 칸입니다.*

국가계약법령과 지방계약법령은 각각 재정경제부와 행정안전부라는 서로 다른 부처가 관장하고 있어, 두 법령의 조항을 비교하려면 문서를 따로 찾아 일일이 대조해야 한다. 예를 들어 공공공사 발주체계 개편 과정에서 안전관리자 배치기준이나 실적인정 기준 등을, 국가계약법의 적용을 받는 중앙정부·공공기관 공사와 지방계약법의 적용을 받는 지방공사 기준으로 정합성 있게 정비하려면 두 법령의 해당 조항을 일일이 맞대어 보아야 한다. 이 때문에 두 부처의 계약제도 담당자는 제도 정비나 질의응답 과정에서 국가계약과 지방계약의 차이를 빠르고 정확하게 확인하기 어렵고, 대조 과정에서 차이를 놓치기도 쉽다. 이 앱은 두 법령을 한 곳에서 비교해 그 차이를 한눈에 확인할 수 있게 하여 이러한 불편을 해결한다.


## 2. 현행 방식과 한계
*지금은 이 일을 어떻게 처리하고 있고, 무엇이 아쉬운지 적는 칸입니다.*

- **현행 방식:** 국가계약법령과 지방계약법령을 각각 법령 원문(국가법령정보센터 등)에서 따로 열어, 눈으로 조항을 오가며 수작업으로 대조한다.
- **한계:**
  - 두 문서를 번갈아 봐야 해서 대조에 시간이 오래 걸린다.
  - 조항 문구의 미세한 차이를 사람이 눈으로 찾다 보면 놓치기 쉽다.
  - 대응 조항이 어디에 있는지 매번 다시 찾아야 해 반복 업무가 많다.
  - 담당자마다 대조 결과가 달라질 수 있어 일관성이 떨어진다.


## 3. 목표와 기대효과 (성공 기준)
*측정 가능한 숫자로 적는 칸입니다 (처리 시간, 문의 건수, 정답률 등).*

> 아래 수치는 이번 실습용 **지향 목표치**이며, 별도 측정 방법은 이번 범위에 포함하지 않는다.

- **대조 시간 단축:** 조항 1쌍을 비교하는 데 걸리는 시간을 기존 수작업 대비 50% 이상 단축한다.
- **차이 식별 정확도:** 좌우 조항의 서로 다른 문구를 90% 이상 정확하게 식별·요약한다.
- **반복 문의 감소:** "국가계약과 지방계약의 이 조항이 어떻게 다른가"에 대한 내부 반복 문의를 도입 전 대비 30% 이상 줄인다.
- **사용 편의:** 담당자가 별도 교육 없이 3분 이내에 원하는 조항 비교 결과를 확인할 수 있다.


## 4. 사용자와 이용 흐름
*누가 쓰는지와 사용 순서를 화살표로 적는 칸입니다.*

- **사용자:** 재정경제부 및 행정안전부의 계약제도 담당자

- **이용 흐름:**
  앱 접속 → 비교할 주제 선택 → **법률·시행령·시행규칙 3계층**이 한 화면에 세로로 표시(각 계층마다 좌=국가/우=지방 조문 나란히) → 계층별 **'핵심 차이 요약'** 으로 무엇이 다른지 확인(참조 별표는 접이식) → (차이가 없으면 "차이 없음" 확인) → 다음 주제 선택


## 5. 주요 기능 (must / nice로 구분)
*꼭 필요한 기능(must)과 있으면 좋은 기능(nice)을 구분해 적는 칸입니다.*

### Must (반드시 만드는 기능)

**1) 조항 나란히 비교 (좌우 대조 뷰)**
- 설명: 같은 주제의 국가계약법령 조항과 지방계약법령 조항을 좌우 두 칼럼으로 나란히 보여준다.
- AI가 꼭 지켜야 할 규칙:
  - 항상 좌측=국가계약법령, 우측=지방계약법령 순서로 고정한다.
  - 한 화면에는 한 개의 비교 주제를 표시하되, 그 주제의 **법률·시행령·시행규칙 3계층**을 세로로 쌓아 각 계층마다 국가/지방을 좌우 비교한다. (변경됨: 기존 단일 조문쌍 → 3계층)
  - 대응 조항은 조문 번호가 아니라 '비교 주제'를 기준으로 짝짓는다.
  - 긴 조문도 별도 스크롤 없이 세로로 전체를 펼쳐 표시한다.
  - 조문이 참조하는 별표(예: 부정당업자 입찰참가자격 제한기준표, 과징금 부과기준표)는 텍스트로 함께 표시하되, 길이가 길어 **접이식(기본 접힘, 펼치면 스크롤)** 으로 둔다.
  - 비교 주제는 제재 관련(입찰참가자격 제한·과징금·지체상금)으로 시작해 **보증금 관련(입찰보증금·계약보증금)까지 5개로 확장**했다. (변경됨)
  - 서로 대응하는 조항이 없으면 빈 칸 대신 "해당 조항 없음"을 명시한다.
- 데이터 출처·범위: 국가계약법 및 동법 시행령·시행규칙, 지방계약법 및 동법 시행령·시행규칙 조문을 **법제처(국가법령정보 공동활용) OpenAPI**로 받아 활용한다. (변경됨: 수동 고정본 → API 수집, **A안 빌드타임 수집** 확정·구현 — DESIGN.md §7 참조)

**2) 핵심 차이 요약 (사실 기반)**  ※ (변경) 초기의 '색상 하이라이트'는 제거하고, 이 텍스트 요약으로 대체함
- 설명: 각 계층에서 국가·지방 조문을 자동 대조해 '무엇이 다른지'를 사실로 요약해 조문 위에 보여준다.
- AI가 꼭 지켜야 할 규칙:
  - 단어 단위 **LCS 정렬**로 서로 다른 구간을 추출한다.
  - 차이를 성격별(**기간·기한 / 비율·금액 / 사유·대상 / 부과·납부 / 문구·용어**)로 분류해 의미있는 차이를 우선 나열한다. 분류는 **키워드 기준 참고용**이다.
  - 개정·신설·삭제 등 `<...>` 표기(개정일자 등)는 요약 대조에서 제외한다.
  - 차이의 **의미·유불리는 판단하지 않는다**(유권해석 금지). "※ 문구 대조 결과이며 법적 해석이 아닙니다." 를 명시한다.
  - 한쪽만 조항이 있으면 "국가/지방에만 있음", 완전히 같으면 "차이 없음"으로 표시한다.
  - 길이가 긴 조항 단위 차이는 건수만 알리고 원문 대조로 유도한다.

**3) 비교 주제 선택 (드롭다운)**
- 설명: 초기 비교 주제(입찰참가자격 제한·과징금·지체상금 등) 중 하나를 목록/드롭다운에서 골라 좌우 비교 화면으로 연결한다.
- AI가 꼭 지켜야 할 규칙:
  - 주제 선택 수단(드롭다운)을 반드시 제공하며, 선택한 주제의 3계층(법률·시행령·시행규칙) 비교를 화면에 표시한다.

### Nice (있으면 좋은 기능 — 이번 필수 범위는 아님)
- (현재 없음)


## 6. 범위와 비범위
*이번 실습에서 만들 것 / 만들지 않을 것을 적는 칸입니다.*

### 범위 (이번에 만들 것)
- 비교 주제 선택 (드롭다운)
- 조항 나란히 비교 (법률·시행령·시행규칙 3계층 좌우 대조 + 참조 별표)
- 핵심 차이 요약 (사실 기반 — 제재수준/보증금 측면 대조, "차이 N건" 배지) (변경됨)
- 주제별 '동향 살펴보기' (공식 출처 링크, 새 탭) (추가됨)
- Vercel 정적 배포 (`dist/`만 서빙 + 보안 헤더) (추가됨)

### 비범위 (이번에 만들지 않을 것)
- 국가·지방 계약법령 외의 다른 법령(예: 세법, 하도급법 등) 비교는 하지 않는다.
- 법제처 API로 조문을 받아오되(변경됨), **실시간 상시 자동 갱신·모니터링**은 이번 범위가 아니다. (예: 빌드타임/수동 트리거로 수집하는 수준까지만)
- AI 자동 유권해석·법률 자문 기능은 만들지 않는다 (앱은 '비교'만, 해석은 사람이).
- 회원가입·로그인·권한관리 등 계정 시스템은 이번엔 만들지 않는다.


## 7. 보안·개인정보 검토
*사용 문서의 공개등급, 개인정보 여부, API 키 관리 방법을 적는 칸입니다.*

- **사용 문서의 공개등급:** 국가계약법령·지방계약법령은 국가법령정보센터에서 제공하는 누구나 열람 가능한 **공개 법령 정보**로, 보안 등급상 제약이 없다.
- **개인정보 여부:** 법령 조문 텍스트만 다루며 **개인정보를 수집·저장·처리하지 않는다.** (회원가입·로그인이 비범위이므로 이용자 개인정보도 다루지 않는다.)
- **API 키 관리 방법:** (변경됨) 법제처 OpenAPI 인증키(OC 등)를 사용한다. 키는 **소스 코드·HTML·커밋에 절대 넣지 않고** 환경변수(`.env`) 파일로만 관리하며(`.gitignore`로 보호), 값 없는 템플릿(`.env.example`)만 공유한다. 순수 클라이언트 HTML에는 키를 두지 않는다(브라우저 노출). 키가 노출되면 즉시 재발급한다.


## 8. 기술 스택
*개발에 사용할 도구 모음을 적는 칸입니다 (난이도별 옵션 중 내가 고른 것과 그 이유).*

- **선택한 구성 (변경됨):** 화면은 여전히 HTML + CSS + 순수 JavaScript로 만든다. 다만 법령 데이터는 파일 내장 대신 **법제처 OpenAPI**에서 받아 활용한다. 키 노출·CORS 문제로 순수 클라이언트 직접 호출은 부적합하므로, **데이터 수집 경로(빌드타임 스크립트 vs 프록시 서버)를 확정**해야 한다(추천: 빌드타임 수집 → `data.json` → 정적 HTML이 읽기). 상세는 DESIGN.md의 API 연동 아키텍처 절 참조. **(확정)** 빌드타임 수집(A안)으로 확정·구현했고 **Vercel 정적 호스팅**으로 배포했다(빌드=`build-dist.js`, 출력=`dist/`).
- **고른 이유:**
  - 파일 하나를 브라우저로 열면 바로 실행되어, 개발이 처음이어도 결과를 눈으로 확인하기 가장 쉽다.
  - 이번 범위가 기능 2개(좌우 비교 + 차이 하이라이트)로 작고, 법령 데이터도 고정본이라 서버·데이터베이스 같은 복잡한 구성이 필요 없다.
  - 회원가입·크롤링·자동 해석 등을 만들지 않기로 했으므로(비범위), 가장 단순한 구성으로 충분하다.


## 9. 개발 단위
*핵심 기능을 만들기 좋은 작은 작업 단위로 나눠, 만들 순서대로 정리한 칸입니다.*

1. HTML 기본 골격과 페이지 틀(제목·비교 영역 자리)을 만든다.
2. 국가법령정보센터 조문 데이터를 주제별로 파일 안에 넣는다(국가/지방 한 쌍씩).
3. 비교할 주제를 고르는 선택 UI(드롭다운)를 만든다.
4. 좌=국가계약법령, 우=지방계약법령의 2칼럼 좌우 레이아웃을 만든다.
5. 선택한 주제의 조문을 좌우에 표시하고, 없으면 "해당 조항 없음"을 표시한다.
6. 좌우 두 조문의 텍스트 차이를 찾아내는 비교(diff) 로직을 만든다.
7. 서로 다른 문구만 1가지 색으로 하이라이트한다(같은 문구는 강조하지 않음).
8. 차이가 전혀 없을 때 "차이 없음" 배지를 표시한다.
9. 남색 계열 스타일과 가독성을 다듬어 PC 화면에 맞춰 정리한다.
